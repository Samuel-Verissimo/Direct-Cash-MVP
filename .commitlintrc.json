{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [2, "always", ["api", "web", "shared", "docker", "ci", "deps", "prisma"]],
    "subject-max-length": [2, "always", 72]
  }
}
